apiVersion: v1
kind: Service
metadata:
  name: counter-backend
  labels:
    app: counter-backend
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: counter-backend